#!/bin/bash -eu

case "$1" in
  -v|--version)
    version="$2"
esac

mkdir -p ${OPENSHIFT_HADOOP_DIR}/template

cp -r ${OPENSHIFT_HADOOP_DIR}/versions/${version}/hadoop ${OPENSHIFT_HADOOP_DIR}/hadoop

